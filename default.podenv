---
parent: emacs-borg
environ:
  BORG_URL: https://github.com/TristanCacqueray/emacs.d -b without-notmuch
mounts:
  ~/.emacs.d: .
pre-tasks:
  - name: Detangle init.org
    command: pushd ~/.emacs.d; make build-init; popd
packages:
  # Python
  - python3-mypy
  # Haskell
  - hlint
